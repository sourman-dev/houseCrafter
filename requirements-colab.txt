# Colab overlay for the Gradio mock UI.
# Do NOT pin torch/torchvision/xformers/flash-attn/pytorch3d.
# Do NOT list open3d here: no wheel on Colab Python 3.13; one missing
# package would abort the entire pip -r install (trimesh never installed).
# Keep Colab's preinstalled CUDA PyTorch.

gradio>=4.20.0
gdown>=5.1.0
trimesh
plyfile
omegaconf
einops
opencv-python-headless
diffusers
accelerate
safetensors
tqdm
pyyaml
matplotlib
imageio
lmdb
networkx
scikit-image
lpips
fire
